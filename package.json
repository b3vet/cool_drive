{
  "name": "cool-drive",
  "version": "0.1.0",
  "description": "A drift-focused open-world 3D driving game built with three.js. Tilt to steer on mobile.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build:web": "node scripts/build-www.mjs",
    "ios": "npm run build:web && npx cap sync ios && npx cap open ios",
    "ios:sync": "npm run build:web && npx cap sync ios",
    "bump": "node scripts/bump-build.mjs"
  },
  "license": "MIT",
  "dependencies": {
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@capacitor/ios": "^8.4.1"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  }
}
